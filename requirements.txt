# The Lambda runtime already includes boto3, and the agent otherwise uses only
# the Python standard library, so there is nothing to bundle for deployment.
# boto3 is pinned here only for local development / running the stub demo.
boto3>=1.34.0
